# 주간보고 PPT — 동국씨엠 공식 양식 재현 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 앱의 주간보고 PPT 출력(`buildReportDeck`)을 동국씨엠 공식 양식(참조 원본 `docs/26.07.02. D-Cube 주간보고…pptx`)과 디자인·글꼴·색·A4 크기 동일하게 재현한다 — 표지 + 「전주/금주 주요활동」서술형 본문 + 이슈·주요이벤트.

**Architecture:** 기존 단일 출처 `WeeklyReportModel`(weekly.ts)에 **전주(prevWeek) 필드만 추가**(Excel 무영향)하고, PPT 전용 변환을 신규 순수함수 `buildWeeklyNarrative`(narrative.ts)로 격리한다. `pptx.ts`는 pptxgenjs 4.0.1 네이티브 도형/표/이미지로 참조 레이아웃을 재현하고, DK 로고·워터마크 PNG는 base64 `.ts` 상수 모듈로 임베드(Next.js/Vercel 번들 안전). 라우트(`/api/report`)·`buildReportDeck(model): Promise<Buffer>` 시그니처는 불변.

**Tech Stack:** TypeScript · pptxgenjs 4.0.1 · Vitest 4.1.9 · Next.js(App Router, `runtime='nodejs'`) · `@/` = `src/` 경로 별칭.

---

## 사전 조건 / 전역 규약

- 테스트 러너: **Vitest**. 전체: `npm test` (= `vitest run`). 단일 파일: `npm test -- <path>` (npm에 `--` 필수).
- import는 항상 `@/lib/...` 별칭(상대경로 금지). 서드파티는 bare specifier.
- 타입체크: `npx tsc --noEmit`. tsconfig `strict:true` — 픽스처 `node()`는 `as ComputedItem` 캐스트 유지.
- 색상 hex는 dkbrand.ts 규약대로 **'#' 없는 6자리**.
- 커밋: 각 Task 끝에서 커밋. 브랜치 `feat/weekly-report-dk-pptx`(이미 생성됨)에서 작업.
- **node_modules 설치 확인**: 조사 중 node_modules 미탐지 경고가 있었음. 첫 테스트 실행 전 `npm ci` 또는 `npm install`로 의존성 보장.

### 파일 구조 (생성/수정 매핑)

| 파일 | 책임 | 조치 |
|------|------|------|
| `src/lib/report/dkbrand.ts` | 브랜드 색 토큰 | 수정(중립 회색 2개 추가) |
| `src/lib/report/assets/dk-logo.png` | 동국씨엠 워드마크 원본 | 생성(참조에서 추출) |
| `src/lib/report/assets/dk-watermark.png` | DK 모노그램 워터마크 원본 | 생성(참조에서 추출) |
| `scripts/gen-report-assets.mjs` | PNG→base64 `.ts` 생성기 | 생성 |
| `src/lib/report/assets/reportImages.ts` | base64 data-URI 상수(자동생성) | 생성 |
| `src/lib/report/weekly.ts` | 단일 출처 모델 | 수정(prevWeek 필드 **추가만**) |
| `src/lib/report/narrative.ts` | PPT용 서술형 변환(순수) | 생성 |
| `src/lib/report/pptx.ts` | pptxgenjs 덱 생성 | **재작성**(구 네이비 덱 제거) |
| `tests/report/weekly.test.ts` | 모델 단위 테스트 | 수정(prevWeek describe 추가) |
| `tests/report/narrative.test.ts` | narrative 단위 테스트 | 생성 |
| `tests/report/exporters.test.ts` | 익스포터 스모크 | 수정(pptx 어서션 신양식화) |

---

## Task 1: 브랜드 색 토큰 추가 (dkbrand.ts)

**Files:**
- Modify: `src/lib/report/dkbrand.ts` (PN 객체, 현재 L8-23)

- [ ] **Step 1: 중립 회색 토큰 2개 추가**

`PN` 객체에서 `line: 'D4D8E0',` 줄 바로 아래에 두 줄을 추가한다(기존 회색은 모두 블루 틴트라 순중립이 없음):

```ts
  line: 'D4D8E0',       // 테두리 · 막대 트랙
  divider: 'A6A6A6',    // 내지 헤더 구분선(중립 회색, 참조 bg1 65%)
  footerGray: 'B6B6B6', // 푸터·페이지번호(중립 회색, 참조 lt2/bg2)
```

- [ ] **Step 2: 타입체크로 회귀 없음 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음(신규 토큰은 순수 추가).

- [ ] **Step 3: 커밋**

```bash
git add src/lib/report/dkbrand.ts
git commit -m "feat(report): dkbrand에 중립 회색 토큰(divider/footerGray) 추가"
```

---

## Task 2: DK 브랜드 자산 임베드 (PNG + base64 모듈)

참조 원본에서 추출한 로고/워터마크 PNG를 리포지토리에 커밋하고, Next.js/Vercel 번들에서 안전한 **base64 `.ts` 상수 모듈**로 변환한다(fs 런타임 읽기·png import는 Vercel에서 불안정 — 조사 결론).

**Files:**
- Create: `src/lib/report/assets/dk-logo.png` (참조 `ppt/media/image2.png`, 229×48)
- Create: `src/lib/report/assets/dk-watermark.png` (참조 `ppt/media/image3.png`, 450×622)
- Create: `scripts/gen-report-assets.mjs`
- Create: `src/lib/report/assets/reportImages.ts` (생성물)

- [ ] **Step 1: 참조 PNG 2개를 자산 폴더로 복사**

이미 추출된 스크래치패드의 참조 미디어에서 복사(경로는 이 세션 스크래치패드 기준):

```bash
mkdir -p src/lib/report/assets
cp "C:/Users/donse/AppData/Local/Temp/claude/C--Users-donse-wbs-web/2e1a654d-77cb-40ba-814b-1b54376f96a4/scratchpad/ref_pptx/ppt/media/image2.png" src/lib/report/assets/dk-logo.png
cp "C:/Users/donse/AppData/Local/Temp/claude/C--Users-donse-wbs-web/2e1a654d-77cb-40ba-814b-1b54376f96a4/scratchpad/ref_pptx/ppt/media/image3.png" src/lib/report/assets/dk-watermark.png
```

> 스크래치패드가 정리됐으면: 원본 `docs/26.07.02. D-Cube 주간보고….pptx`를 `.zip`으로 복사→`unzip`하여 `ppt/media/image2.png`(로고)·`image3.png`(워터마크)를 다시 추출.

- [ ] **Step 2: 생성기 스크립트 작성**

`scripts/gen-report-assets.mjs`:

```js
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs'
import { join, dirname } from 'node:path'
import { fileURLToPath } from 'node:url'

const root = join(dirname(fileURLToPath(import.meta.url)), '..')
const uri = rel => 'data:image/png;base64,' + readFileSync(join(root, rel)).toString('base64')

const LOGO_PNG = uri('src/lib/report/assets/dk-logo.png')       // 229x48 (ratio 4.77)
const WATERMARK_PNG = uri('src/lib/report/assets/dk-watermark.png') // 450x622 (ratio 0.723)

const out =
  '// AUTO-GENERATED by scripts/gen-report-assets.mjs — DO NOT EDIT.\n' +
  '// 원본 PNG 변경 시 `node scripts/gen-report-assets.mjs` 재실행 후 커밋.\n' +
  `export const LOGO_PNG = ${JSON.stringify(LOGO_PNG)}\n` +
  `export const WATERMARK_PNG = ${JSON.stringify(WATERMARK_PNG)}\n`

mkdirSync(join(root, 'src/lib/report/assets'), { recursive: true })
writeFileSync(join(root, 'src/lib/report/assets/reportImages.ts'), out)
console.log('wrote src/lib/report/assets/reportImages.ts (%d + %d bytes b64)', LOGO_PNG.length, WATERMARK_PNG.length)
```

- [ ] **Step 3: 생성기 실행 → reportImages.ts 생성**

Run: `node scripts/gen-report-assets.mjs`
Expected: `wrote src/lib/report/assets/reportImages.ts ...` 출력, 파일에 `export const LOGO_PNG = 'data:image/png;base64,iVBOR...'` 두 상수 존재.

- [ ] **Step 4: 생성 모듈이 유효한 data URI인지 스모크 확인**

Run:
```bash
node -e "const m=require('node:fs').readFileSync('src/lib/report/assets/reportImages.ts','utf8'); if(!/LOGO_PNG = 'data:image\/png;base64,[A-Za-z0-9+/]{100,}/.test(m)) throw new Error('LOGO bad'); if(!/WATERMARK_PNG = 'data:image\/png;base64,[A-Za-z0-9+/]{100,}/.test(m)) throw new Error('WM bad'); console.log('ok')"
```
Expected: `ok`

- [ ] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 6: 커밋**

```bash
git add src/lib/report/assets/dk-logo.png src/lib/report/assets/dk-watermark.png scripts/gen-report-assets.mjs src/lib/report/assets/reportImages.ts
git commit -m "feat(report): DK 로고/워터마크 base64 자산 모듈 + 생성기"
```

---

## Task 3: weekly.ts에 전주(prevWeek) 데이터 추가 (TDD, 추가만)

> 🔴 **불변식:** 기존 필드(`thisWeek`/`nextWeek` 등) 정의 변경 금지 — `excel.ts`가 소비함. **새 필드만 추가.**

**Files:**
- Modify: `src/lib/report/weekly.ts`
  - `WeeklyMeta` 인터페이스 (L18-35)
  - `PhasePlanActual` 인터페이스 (L75-81)
  - 주차 date math 블록 (L268-287)
  - planActual `roots.map` (L351-357)
  - 최종 return의 meta 리터럴 (L456-463, week 필드 나열 L461)
- Test: `tests/report/weekly.test.ts` (기존 파일 끝에 describe 추가; node()/phase() 헬퍼는 이미 존재)

- [ ] **Step 1: 실패 테스트 작성 — 전주 활동 추출**

`tests/report/weekly.test.ts` **맨 끝**에 추가(파일 상단의 기존 `node`/`phase` 헬퍼 재사용):

```ts
describe('prevWeek — 전주 주요활동', () => {
  // today=2026-07-07(화) → 이번주 월=7/6, 지난주 월=6/29~7/5
  const items: ComputedItem[] = [
    phase('설계', [
      node({ name: '전주완료작업', status: 'done', rolledActualPct: 100, owners: [{ team: 'ERP', kind: 'primary' }], plannedStart: '2026-06-29', plannedEnd: '2026-07-03' }),
      node({ name: '금주진행작업', status: 'in_progress', rolledActualPct: 50, owners: [{ team: 'MES', kind: 'primary' }], plannedStart: '2026-07-06', plannedEnd: '2026-07-10' }),
      node({ name: '미착수작업', status: 'not_started', rolledActualPct: 0, owners: [], plannedStart: '2026-06-29', plannedEnd: '2026-07-03' }),
    ], { weight: 1, plannedPct: 100, rolledActualPct: 60 }),
  ]
  const project = { name: 'D-CUBE PI', description: null, start_date: null, end_date: null }
  const m = buildWeeklyReportModel(items, project, '2026-07-07')

  it('meta에 전주 범위/기간이 있다', () => {
    expect(m.meta.prevWeekRange).toBe('6/29~7/5')
    expect(m.meta.prevWeekStart).toBe('2026-06-29')
    expect(m.meta.prevWeekDays).toHaveLength(5)
    expect(m.meta.prevWeekDays[0]).toBe('2026-06-29')
  })
  it('전주 겹침+진행/완료 작업만 prevWeek에 담기고, 미착수는 제외', () => {
    const names = m.planActual.flatMap(p => p.prevWeek.map(t => t.name))
    expect(names).toContain('전주완료작업')
    expect(names).not.toContain('미착수작업')
    expect(names).not.toContain('금주진행작업')
  })
  it('기존 thisWeek(진행중) 정의는 불변 — 금주진행작업만', () => {
    const names = m.planActual.flatMap(p => p.thisWeek.map(t => t.name))
    expect(names).toEqual(['금주진행작업'])
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npm test -- tests/report/weekly.test.ts -t "prevWeek"`
Expected: FAIL — `m.meta.prevWeekRange`는 undefined, `p.prevWeek`는 존재하지 않음(타입/런타임 에러).

- [ ] **Step 3: WeeklyMeta 인터페이스에 전주 필드 추가**

`weekly.ts` L18-35 `WeeklyMeta`에서 `nextWeekDays: string[]` 줄 아래에 추가:

```ts
  nextWeekDays: string[]     // 차주 월~금 5개
  prevWeekStart: string      // 지난주 월요일 'YYYY-MM-DD'
  prevWeekDays: string[]     // 지난주 월~금 5개
  prevWeekRange: string      // '6/29~7/5'
```

- [ ] **Step 4: PhasePlanActual 인터페이스에 prevWeek 추가**

`weekly.ts` L75-81 `PhasePlanActual`에서 `thisWeek: WeeklyTaskRow[]` 줄 위에 추가:

```ts
export interface PhasePlanActual {
  phaseName: string
  plannedPct: number
  actualPct: number
  prevWeek: WeeklyTaskRow[]
  thisWeek: WeeklyTaskRow[]
  nextWeek: WeeklyTaskRow[]
}
```

- [ ] **Step 5: 주차 date math에 전주 계산 추가**

`weekly.ts` 주차 블록(L268-287)에서 `const nextEndD = addDays(weekStartD, 13)` 줄 아래에 추가:

```ts
  const nextEndD = addDays(weekStartD, 13)
  const prevStartD = addDays(weekStartD, -7)
  const prevEndD = addDays(weekStartD, -1)
```

그리고 `const nextWeekEnd = fmtUTC(nextEndD)` 줄 아래에 추가:

```ts
  const nextWeekEnd = fmtUTC(nextEndD)
  const prevWeekStart = fmtUTC(prevStartD)
  const prevWeekEnd = fmtUTC(prevEndD)
```

그리고 `nextWeekDays`/`nextWeekRange` 계산 아래에 추가:

```ts
  const nextWeekRange = `${md(nextStartD)}~${md(nextEndD)}`
  const prevWeekDays = Array.from({ length: 5 }, (_, i) => fmtUTC(addDays(prevStartD, i)))
  const prevWeekRange = `${md(prevStartD)}~${md(prevEndD)}`
```

- [ ] **Step 6: prevWeekLeaves 계산 추가**

`weekly.ts`의 `nextWeekLeaves` 계산(L312-314) 아래에 추가(전주와 겹치고 미착수가 아닌 leaf = "지난주에 손댄/끝낸 작업"):

```ts
  const prevWeekLeaves = leaves.filter(
    l => l.node.status !== 'not_started' && overlaps(l.node.plannedStart, l.node.plannedEnd, prevWeekStart, prevWeekEnd),
  )
```

- [ ] **Step 7: planActual에 prevWeek 매핑 추가**

`weekly.ts` planActual `roots.map`(L351-357)에서 `thisWeek:` 줄 위에 prevWeek 줄 추가:

```ts
  const planActual: PhasePlanActual[] = roots.map(r => ({
    phaseName: r.name,
    plannedPct: r.plannedPct,
    actualPct: r.rolledActualPct,
    prevWeek: prevWeekLeaves.filter(l => l.rootName === r.name).map(toTaskRow),
    thisWeek: leaves.filter(l => l.rootName === r.name && l.node.status === 'in_progress').map(toTaskRow),
    nextWeek: nextWeekLeaves.filter(l => l.rootName === r.name).map(toTaskRow),
  }))
```

- [ ] **Step 8: return의 meta 리터럴에 전주 필드 추가**

`weekly.ts` 최종 return(L456-463)에서 week 필드 나열 줄(L461)을 확장:

```ts
      weekRange, nextWeekRange, weekStart, weekDays, nextWeekStart, nextWeekDays,
      prevWeekStart, prevWeekDays, prevWeekRange,
```

- [ ] **Step 9: 테스트 통과 확인**

Run: `npm test -- tests/report/weekly.test.ts`
Expected: PASS(신규 prevWeek describe 3건 + 기존 테스트 전부).

- [ ] **Step 10: Excel 무영향 회귀 확인**

Run: `npm test -- tests/report/exporters.test.ts -t "excel"`
Expected: PASS(기존 Excel 어서션 불변 — thisWeek 정의 안 바꿨으므로).

- [ ] **Step 11: 커밋**

```bash
git add src/lib/report/weekly.ts tests/report/weekly.test.ts
git commit -m "feat(report): 주간모델에 전주(prevWeek) 활동 필드 추가 (추가만, Excel 무영향)"
```

---

## Task 4: narrative.ts — PPT용 서술형 변환 (TDD)

**Files:**
- Create: `src/lib/report/narrative.ts`
- Test: `tests/report/narrative.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`tests/report/narrative.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { buildWeeklyReportModel } from '@/lib/report/weekly'
import { buildWeeklyNarrative } from '@/lib/report/narrative'
import type { ComputedItem, Meeting } from '@/lib/domain/types'

const node = (over: Partial<ComputedItem>): ComputedItem =>
  ({
    id: Math.random().toString(36).slice(2), parentId: null, level: 'activity', code: '1', sortOrder: 1,
    name: 'n', biz: null, deliverable: null, plannedStart: null, plannedEnd: null, weight: null, actualPct: null,
    owners: [], plannedPct: 0, rolledActualPct: 0, achievement: null, status: 'not_started', children: [],
    ...over,
  }) as ComputedItem
const phase = (name: string, children: ComputedItem[], over: Partial<ComputedItem> = {}): ComputedItem =>
  node({ level: 'phase', name, children, ...over })

const items: ComputedItem[] = [
  phase('설계', [
    node({ name: '전주완료작업', status: 'done', rolledActualPct: 100, owners: [{ team: 'ERP', kind: 'primary' }], plannedStart: '2026-06-29', plannedEnd: '2026-07-03' }),
    node({ name: '금주진행작업', status: 'in_progress', rolledActualPct: 50, owners: [{ team: 'MES', kind: 'primary' }], plannedStart: '2026-07-06', plannedEnd: '2026-07-10' }),
  ], { weight: 1, plannedPct: 100, rolledActualPct: 60 }),
]
const project = { name: 'D-CUBE PI', description: null, start_date: null, end_date: null }

describe('buildWeeklyNarrative', () => {
  const m = buildWeeklyReportModel(items, project, '2026-07-07')
  const n = buildWeeklyNarrative(m)

  it('prev/curr가 Phase 그룹으로 나오고 빈 Phase는 제외', () => {
    expect(n.prev.map(g => g.phase)).toContain('설계')
    expect(n.prev[0].items.some(s => s.includes('전주완료작업'))).toBe(true)
    expect(n.curr[0].items.some(s => s.includes('금주진행작업'))).toBe(true)
  })
  it('활동 항목 문구에 담당·상태·공정율 포함', () => {
    const line = n.curr[0].items[0]
    expect(line).toContain('금주진행작업')
    expect(line).toContain('MES')
    expect(line).toContain('진행중')
    expect(line).toContain('50%')
  })
  it('이슈/이벤트는 문자열 배열', () => {
    expect(Array.isArray(n.issues)).toBe(true)
    expect(Array.isArray(n.events)).toBe(true)
  })
  it('회의가 있으면 events에 반영', () => {
    const meetings: Meeting[] = [
      { id: 'mtg1', projectId: 'p', title: 'Kick-Off', location: '대회의실', startDate: '2026-07-10', endDate: null, startTime: '14:00', endTime: '15:00', rrule: null, attendeeCount: 8, weekday: null } as unknown as Meeting,
    ]
    const m2 = buildWeeklyReportModel(items, project, '2026-07-07', { meetings })
    const n2 = buildWeeklyNarrative(m2)
    expect(n2.events.some(e => e.includes('Kick-Off'))).toBe(true)
  })
})
```

> ⚠️ `Meeting` 픽스처 형태는 `@/lib/domain/types`의 실제 `Meeting`에 맞춰 조정(위는 예시 캐스트). 회의가 전개되지 않으면 이 한 케이스는 fixture 필드만 맞추면 됨 — 핵심은 prev/curr/issues/events 4필드.

- [ ] **Step 2: 실패 확인**

Run: `npm test -- tests/report/narrative.test.ts`
Expected: FAIL — `buildWeeklyNarrative`/`@/lib/report/narrative` 없음.

- [ ] **Step 3: narrative.ts 구현**

`src/lib/report/narrative.ts`:

```ts
import type { WeeklyReportModel, WeeklyTaskRow, PhasePlanActual } from './weekly'
import { statusKr } from './weekly'

export interface NarrativeGroup {
  phase: string
  items: string[]
}
export interface NarrativeModel {
  prev: NarrativeGroup[]   // 전주 주요활동 (Phase별)
  curr: NarrativeGroup[]   // 금주 주요활동 (Phase별)
  issues: string[]         // 이슈사항
  events: string[]         // 주요 이벤트(회의·마일스톤)
}

/** 작업 1건 → '작업명 · 담당 · 상태 NN%'. */
function taskLine(r: WeeklyTaskRow): string {
  return `${r.name} · ${r.ownerText} · ${statusKr(r.status)} ${r.actualPct}%`
}

/** planActual에서 지정 컬럼(prevWeek|thisWeek)을 Phase 그룹으로. 빈 Phase 제외. */
function groupsOf(planActual: PhasePlanActual[], key: 'prevWeek' | 'thisWeek'): NarrativeGroup[] {
  return planActual
    .map(p => ({ phase: p.phaseName, items: p[key].map(taskLine) }))
    .filter(g => g.items.length > 0)
}

/** 주간 모델 → PPT 서술형 변환(순수·결정적). */
export function buildWeeklyNarrative(model: WeeklyReportModel): NarrativeModel {
  const prev = groupsOf(model.planActual, 'prevWeek')
  const curr = groupsOf(model.planActual, 'thisWeek')
  const issues = model.issues.map(i => i.content)
  const events = [...model.meetings.thisWeek, ...model.meetings.nextWeek].map(mtg =>
    `${mtg.date} ${mtg.title}${mtg.location && mtg.location !== '-' ? ` (${mtg.location})` : ''}`,
  )
  return { prev, curr, issues, events }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test -- tests/report/narrative.test.ts`
Expected: PASS(회의 케이스가 fixture 형태로 실패하면 fixture만 실제 `Meeting` 타입에 맞춰 조정 후 재실행).

- [ ] **Step 5: 커밋**

```bash
git add src/lib/report/narrative.ts tests/report/narrative.test.ts
git commit -m "feat(report): buildWeeklyNarrative — 전주/금주 활동·이슈·이벤트 변환(순수)"
```

---

## Task 5: pptx.ts 재작성 — 동국씨엠 공식 표지 + 본문

구 네이비 덱을 제거하고, 참조 레이아웃(A4 · 본고딕 · 흰 표지 + 서술형 본문)을 재현한다. `buildReportDeck(model): Promise<Buffer>` 시그니처 불변(라우트 무변경).

**Files:**
- Rewrite: `src/lib/report/pptx.ts` (전체 교체)

- [ ] **Step 1: pptx.ts 전체 교체**

`src/lib/report/pptx.ts` 전체를 아래로 대체:

```ts
import PptxGenJS from 'pptxgenjs'
import type { WeeklyReportModel } from './weekly'
import { buildWeeklyNarrative, type NarrativeGroup, type NarrativeModel } from './narrative'
import { PN } from './dkbrand'
import { LOGO_PNG, WATERMARK_PNG } from './assets/reportImages'

/* 동국씨엠 공식 주간보고 양식(참조 재현). A4 가로 · 본고딕 · DK 네이비/레드. */
const FONTS = { bold: '본고딕 Bold', medium: '본고딕 Medium', normal: '본고딕 Normal' } as const
const COMPANY = '동국씨엠'
const W = 10.833
const H = 7.5
const BODY_L = 0.30
const BODY_W = 10.23
const LINE_BUDGET = 18 // 본문 활동 컬럼 페이지당 최대 줄 수(넘치면 다음 슬라이드)

type Slide = PptxGenJS.Slide

/** '#'없는 6자리 hex 선형보간. */
function hexLerp(a: string, b: string, t: number): string {
  const ai = parseInt(a, 16), bi = parseInt(b, 16)
  const ar = (ai >> 16) & 255, ag = (ai >> 8) & 255, ab = ai & 255
  const br = (bi >> 16) & 255, bg = (bi >> 8) & 255, bb = bi & 255
  const r = Math.round(ar + (br - ar) * t)
  const g = Math.round(ag + (bg - ag) * t)
  const bl = Math.round(ab + (bb - ab) * t)
  return ((r << 16) | (g << 8) | bl).toString(16).padStart(6, '0')
}

/** 상단 red→navy 그라데이션 룰(스텝 사각형; pptxgenjs 4.0.1은 gradient 미지원). */
function gradientRule(pptx: PptxGenJS, slide: Slide, y: number, h: number) {
  const steps = 20
  const rampEnd = 0.40 // 40% 지점까지 red→navy, 이후 navy
  const seg = W / steps
  for (let i = 0; i < steps; i++) {
    const t = Math.min(1, (i / (steps - 1)) / rampEnd)
    slide.addShape(pptx.ShapeType.rect, { x: i * seg, y, w: seg + 0.02, h, fill: { color: hexLerp(PN.red, PN.navy, t) }, line: { type: 'none' } })
  }
}

/** Phase 그룹들 → addText 런 배열(▐ Phase / ‒ 작업). */
function groupsToRuns(groups: NarrativeGroup[]): PptxGenJS.TextProps[] {
  if (!groups.length) return [{ text: '(해당 없음)', options: { fontFace: FONTS.normal, fontSize: 10, color: PN.subtle } }]
  const runs: PptxGenJS.TextProps[] = []
  groups.forEach((g, gi) => {
    runs.push({ text: `▐ ${g.phase}`, options: { fontFace: FONTS.medium, fontSize: 11, color: PN.navy, bold: true, breakLine: true, paraSpaceBefore: gi ? 4 : 0 } })
    g.items.forEach(it => runs.push({ text: `    ‒ ${it}`, options: { fontFace: FONTS.normal, fontSize: 9.5, color: PN.body, breakLine: true } }))
  })
  return runs
}

interface Page { prev: NarrativeGroup[]; curr: NarrativeGroup[] }

/** prev/curr Phase 그룹을 줄 예산 기준으로 페이지 분할(보통 1페이지). */
function packGroups(prev: NarrativeGroup[], curr: NarrativeGroup[], budget: number): Page[] {
  const phases: string[] = []
  for (const g of prev) if (!phases.includes(g.phase)) phases.push(g.phase)
  for (const g of curr) if (!phases.includes(g.phase)) phases.push(g.phase)
  const pMap = new Map(prev.map(g => [g.phase, g]))
  const cMap = new Map(curr.map(g => [g.phase, g]))
  const cost = (g?: NarrativeGroup) => (g ? 1 + g.items.length : 0)
  const pages: Page[] = []
  let cur: Page = { prev: [], curr: [] }
  let lines = 0
  for (const ph of phases) {
    const pg = pMap.get(ph)
    const cg = cMap.get(ph)
    const c = Math.max(cost(pg), cost(cg))
    if (lines > 0 && lines + c > budget) { pages.push(cur); cur = { prev: [], curr: [] }; lines = 0 }
    if (pg) cur.prev.push(pg)
    if (cg) cur.curr.push(cg)
    lines += c
  }
  pages.push(cur)
  return pages.length ? pages : [{ prev: [], curr: [] }]
}

/* ── 표지 ── 흰 배경 + 상단 red→navy 룰 + 우측 워터마크 + 대형 네이비 제목 + 하단 로고. */
function coverSlide(pptx: PptxGenJS, model: WeeklyReportModel) {
  const { meta } = model
  const slide = pptx.addSlide()
  slide.background = { color: PN.white }
  gradientRule(pptx, slide, 0, 0.045)
  slide.addImage({ data: WATERMARK_PNG, x: 8.251, y: 2.684, w: 2.583, h: 3.570 })
  slide.addText(`${meta.projectName} 주간보고`, { x: 0.484, y: 2.489, w: 5.903, h: 1.046, fontFace: FONTS.bold, fontSize: 32, color: PN.navy, bold: true, valign: 'bottom', charSpacing: -0.5 })
  slide.addText(`${COMPANY} · ${meta.weekLabel}`, { x: 0.484, y: 4.017, w: 5.865, h: 0.30, fontFace: FONTS.medium, fontSize: 14, color: PN.body, valign: 'middle' })
  slide.addImage({ data: LOGO_PNG, x: 4.896, y: 7.079, w: 1.041, h: 0.218 })
}

/* ── 내지 크롬 ── 헤더 제목 + 구분선 + 좌하단 로고 + 우하단 작성자/페이지. */
function contentChrome(pptx: PptxGenJS, model: WeeklyReportModel, page: number, totalPages: number): Slide {
  const slide = pptx.addSlide()
  slide.background = { color: PN.white }
  slide.addText(`${model.meta.projectName} 주간보고`, { x: 0.298, y: 0.187, w: 6.75, h: 0.404, fontFace: FONTS.medium, fontSize: 19, color: PN.navy, valign: 'middle' })
  slide.addShape(pptx.ShapeType.line, { x: 0.295, y: 0.591, w: 10.239, h: 0, line: { color: PN.divider, width: 0.5 } })
  slide.addImage({ data: LOGO_PNG, x: 0.299, y: 7.214, w: 0.828, h: 0.174 })
  slide.addText(`작성자_${COMPANY}`, { x: 7.6, y: 7.22, w: 2.0, h: 0.163, fontFace: FONTS.normal, fontSize: 8, color: PN.footerGray, align: 'right', valign: 'middle' })
  slide.addText(`${page} / ${totalPages}`, { x: 9.7, y: 7.22, w: 0.83, h: 0.161, fontFace: FONTS.normal, fontSize: 8, color: PN.footerGray, align: 'right', valign: 'middle' })
  return slide
}

/* ── 본문 ── 전주/금주 2단 활동 표 + (마지막 페이지) 이슈·주요이벤트. */
function contentBody(pptx: PptxGenJS, slide: Slide, page: Page, model: WeeklyReportModel, narr: NarrativeModel, showIssues: boolean) {
  const labelW = 0.9
  const colW = (BODY_W - labelW) / 2
  const cell = (runs: PptxGenJS.TextProps[] | string, opts: PptxGenJS.TableCellProps): PptxGenJS.TableCell =>
    ({ text: runs as PptxGenJS.TableCell['text'], options: opts })
  const headOpt: PptxGenJS.TableCellProps = { fill: { color: PN.navy }, color: PN.white, bold: true, align: 'center', valign: 'middle', fontFace: FONTS.medium, fontSize: 11 }
  const head: PptxGenJS.TableRow = [
    cell('구분', headOpt),
    cell(`전주 주요활동 (${model.meta.prevWeekRange})`, headOpt),
    cell(`금주 주요활동 (${model.meta.weekRange})`, headOpt),
  ]
  const body: PptxGenJS.TableRow = [
    cell('내용', { fill: { color: PN.zebra }, color: PN.gray, bold: true, align: 'center', valign: 'middle', fontFace: FONTS.medium, fontSize: 10 }),
    cell(groupsToRuns(page.prev), { fill: { color: PN.white }, valign: 'top', margin: 6 }),
    cell(groupsToRuns(page.curr), { fill: { color: PN.white }, valign: 'top', margin: 6 }),
  ]
  slide.addTable([head, body], {
    x: BODY_L, y: 0.75, w: BODY_W, colW: [labelW, colW, colW], rowH: [0.36, 4.35],
    border: { type: 'solid', color: PN.line, pt: 1 }, valign: 'top', fontFace: FONTS.normal, autoPage: false,
  })

  if (!showIssues) return
  const yi = 5.42
  slide.addShape(pptx.ShapeType.rect, { x: BODY_L, y: yi, w: BODY_W, h: 0.32, fill: { color: PN.navy2 }, line: { type: 'none' } })
  slide.addText('이슈사항 및 주요 이벤트', { x: BODY_L + 0.12, y: yi, w: BODY_W - 0.24, h: 0.32, fontFace: FONTS.medium, fontSize: 11, color: PN.white, bold: true, valign: 'middle' })
  const issueRuns: PptxGenJS.TextProps[] = []
  issueRuns.push({ text: '▐ 이슈', options: { fontFace: FONTS.medium, fontSize: 10.5, color: PN.red, bold: true, breakLine: true } })
  const issues = narr.issues.length ? narr.issues : ['특이 이슈 없음']
  issues.forEach(s => issueRuns.push({ text: `    ‒ ${s}`, options: { fontFace: FONTS.normal, fontSize: 9.5, color: PN.body, breakLine: true } }))
  issueRuns.push({ text: '▐ 주요 이벤트', options: { fontFace: FONTS.medium, fontSize: 10.5, color: PN.navy, bold: true, breakLine: true, paraSpaceBefore: 6 } })
  const events = narr.events.length ? narr.events : ['예정된 주요 이벤트 없음']
  events.forEach(s => issueRuns.push({ text: `    ‒ ${s}`, options: { fontFace: FONTS.normal, fontSize: 9.5, color: PN.body, breakLine: true } }))
  slide.addText(issueRuns, { x: BODY_L, y: yi + 0.42, w: BODY_W, h: 1.55, valign: 'top' })
}

/** 주간 공정보고 모델 → 동국씨엠 공식 양식 PPTX(nodebuffer). */
export async function buildReportDeck(model: WeeklyReportModel): Promise<Buffer> {
  const narr = buildWeeklyNarrative(model)
  const pptx = new PptxGenJS()
  pptx.defineLayout({ name: 'DK_A4', width: W, height: H })
  pptx.layout = 'DK_A4'
  pptx.author = COMPANY
  pptx.company = COMPANY
  pptx.title = `${model.meta.projectName} 주간보고`

  const pages = packGroups(narr.prev, narr.curr, LINE_BUDGET)
  const totalPages = 1 + pages.length // 표지 + 본문 N

  coverSlide(pptx, model)
  pages.forEach((pg, i) => {
    const slide = contentChrome(pptx, model, i + 2, totalPages)
    contentBody(pptx, slide, pg, model, narr, i === pages.length - 1)
  })

  return (await pptx.write({ outputType: 'nodebuffer' })) as Buffer
}
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음. (만약 `PptxGenJS.TextProps`/`TableCell`/`TableCellProps` 이름이 4.0.1 타입과 불일치하면, 기존 코드가 쓰던 `PptxGenJS.TableRow`/인라인 객체 패턴에 맞춰 로컬 타입을 조정 — 인터페이스 명은 설치 타입 기준으로 확정.)

- [ ] **Step 3: 커밋**

```bash
git add src/lib/report/pptx.ts
git commit -m "feat(report): PPT를 동국씨엠 공식 양식으로 재작성 (표지+전주/금주 본문, A4/본고딕/DK)"
```

---

## Task 6: 익스포터 스모크 테스트 신양식화 + 전체 검증

기존 `exporters.test.ts`의 pptx 어서션은 구 양식(슬라이드 ≥3, '(3/3)' 마커)을 검사하므로 신양식(표지+본문)에 맞게 갱신한다.

**Files:**
- Modify: `tests/report/exporters.test.ts` (pptx `describe` 블록)

- [ ] **Step 1: 현재 exporters.test.ts 읽기**

Run: 편집 대상 확인을 위해 `tests/report/exporters.test.ts`를 연다. `buildReportDeck`를 다루는 `describe(...)` 블록과, 구 양식 전제(슬라이드 수 `>= 3`, 문자열 `'(3/3)'`, 근태/회의 관련 텍스트)를 사용하는 어서션을 식별한다.

- [ ] **Step 2: pptx describe 블록을 신양식 어서션으로 교체**

`buildReportDeck` 관련 `describe(...)` 블록 전체를 아래로 교체(Excel 관련 describe·import·픽스처는 유지):

```ts
describe('buildReportDeck — 동국씨엠 공식 양식(pptx)', () => {
  const model = buildWeeklyReportModel(items, project, '2026-07-07', { generatedAt: '2026-07-07 09:00' })

  it('유효한 pptx(zip) — PK 시그니처 + 크기', async () => {
    const buf = await buildReportDeck(model)
    expect(buf.length).toBeGreaterThan(1000)
    expect(buf[0]).toBe(0x50)
    expect(buf[1]).toBe(0x4b)
  })

  it('표지+본문 최소 2슬라이드, 슬라이드마다 배경 유지', async () => {
    const buf = await buildReportDeck(model)
    const zip = await JSZip.loadAsync(buf)
    const slidePaths = Object.keys(zip.files).filter(p => /^ppt\/slides\/slide\d+\.xml$/.test(p))
    expect(slidePaths.length).toBeGreaterThanOrEqual(2)
    const xmls = await Promise.all(slidePaths.map(p => zip.files[p].async('string')))
    for (const [i, xml] of xmls.entries()) {
      expect(xml, `slide ${i + 1} 배경 누락`).toContain('<p:bg>')
    }
  })

  it('핵심 문구 포함(제목·회사·전주/금주 헤더)', async () => {
    const buf = await buildReportDeck(model)
    const zip = await JSZip.loadAsync(buf)
    const slidePaths = Object.keys(zip.files).filter(p => /^ppt\/slides\/slide\d+\.xml$/.test(p))
    const joined = (await Promise.all(slidePaths.map(p => zip.files[p].async('string')))).join('')
    expect(joined).toContain('주간보고')
    expect(joined).toContain('동국씨엠')
    expect(joined).toContain('전주 주요활동')
    expect(joined).toContain('금주 주요활동')
  })

  it('A4 레이아웃(슬라이드 크기)이 적용됨', async () => {
    const buf = await buildReportDeck(model)
    const zip = await JSZip.loadAsync(buf)
    const pres = await zip.files['ppt/presentation.xml'].async('string')
    // A4: 10.833in=9906000, 7.5in=6858000 EMU (근사 허용)
    expect(pres).toMatch(/sldSz[^>]*cx="99\d{5}"/)
  })
})
```

> `items`/`project` 픽스처가 exporters.test.ts에 이미 있으면 재사용. 없으면 Task 4 narrative.test.ts의 픽스처(node/phase + items/project)를 이 파일 상단에도 붙여넣는다(관례상 파일별 중복 허용).

- [ ] **Step 3: 익스포터 테스트 통과 확인**

Run: `npm test -- tests/report/exporters.test.ts`
Expected: PASS(Excel + 신양식 pptx).

- [ ] **Step 4: 전체 테스트 스위트**

Run: `npm test`
Expected: 전 스위트 PASS.

- [ ] **Step 5: 타입체크 + 빌드**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

Run: `npm run build`
Expected: Next 빌드 성공(라우트 `/api/report` 포함, `runtime='nodejs'` 유지).

- [ ] **Step 6: 커밋**

```bash
git add tests/report/exporters.test.ts
git commit -m "test(report): pptx 스모크를 동국씨엠 공식 양식(표지+전주/금주)으로 갱신"
```

---

## Task 7: 실제 샘플 PPT 생성 → 참조와 육안 비교·조정

브라우저 목업이 아니라 실제 .pptx를 만들어 참조 원본과 비교한다(디자인·글꼴·간격 미세 조정).

**Files:**
- Temp: 스크래치패드에 샘플 .pptx 출력(리포지토리에 커밋 안 함)

- [ ] **Step 1: 샘플 출력용 임시 테스트 추가**

`tests/report/exporters.test.ts`에 임시 it 추가(환경변수 가드 — 평소엔 스킵):

```ts
it.runIf(process.env.EMIT_SAMPLE === '1')('샘플 pptx를 스크래치패드에 출력', async () => {
  const { writeFileSync } = await import('node:fs')
  const buf = await buildReportDeck(buildWeeklyReportModel(items, project, '2026-07-07', { generatedAt: '2026-07-07 09:00' }))
  writeFileSync('C:/Users/donse/AppData/Local/Temp/claude/C--Users-donse-wbs-web/2e1a654d-77cb-40ba-814b-1b54376f96a4/scratchpad/sample-weekly.pptx', buf)
  expect(buf.length).toBeGreaterThan(1000)
})
```

- [ ] **Step 2: 샘플 생성 실행**

Run: `EMIT_SAMPLE=1 npm test -- tests/report/exporters.test.ts -t "샘플"`
Expected: PASS + `scratchpad/sample-weekly.pptx` 생성.

- [ ] **Step 3: 참조와 비교(가능하면 이미지 렌더)**

LibreOffice가 있으면 표지/본문을 PNG로 변환해 참조 썸네일과 비교:
```bash
soffice --headless --convert-to png --outdir <scratch>/render <scratch>/sample-weekly.pptx  # 있을 때만
```
없으면 사용자가 PowerPoint로 직접 열어 확인. 점검 항목:
- 표지: 흰 배경 · 상단 red→navy 룰 · 우측 워터마크 위치/크기 · 네이비 제목(본고딕 Bold) · 하단중앙 로고.
- 본문: 헤더 제목 · 구분선 · 「전주/금주」2단 표 · ▐/‒ 불릿 · 좌하단 로고 · 우하단 작성자/페이지.

- [ ] **Step 4: 미세 조정(필요 시)**

좌표/폰트/간격 차이가 있으면 `pptx.ts` 상수·좌표를 조정하고 Step 2 재실행. (본고딕 미설치 PC면 대체 렌더될 수 있음 — 조사 §리스크대로 참조 제작 환경 가정.)

- [ ] **Step 5: 임시 테스트 제거 + 커밋(조정분)**

Step 1의 임시 it 제거. 조정한 pptx.ts가 있으면 커밋:
```bash
git add src/lib/report/pptx.ts tests/report/exporters.test.ts
git commit -m "fix(report): 샘플 대조로 표지/본문 좌표·간격 미세 조정"
```

---

## Task 8: 마무리 검증 (라우트 무변경 확인 + 최종 스위트)

**Files:** (수정 없음 — 검증만)

- [ ] **Step 1: 라우트 무변경 확인**

Run: `git diff --stat main -- src/app/api/report/route.ts`
Expected: 변경 없음(빈 diff) — `buildReportDeck(model): Promise<Buffer>` 시그니처 유지로 라우트 무영향.

- [ ] **Step 2: 최종 전체 검증**

Run: `npm test` → PASS
Run: `npx tsc --noEmit` → 에러 없음
Run: `npm run build` → 성공

- [ ] **Step 3: 구 네이비 덱 잔재 없음 확인**

Run: `git grep -n "DK_WIDE\|summarySlide\|attendanceTable\|meetingSlide" -- src/lib/report/pptx.ts`
Expected: 매치 없음(구 덱 함수 완전 제거됨).

---

## Self-Review 결과 (작성자 점검)

- **Spec 커버리지:** 표지(Task5)·본문 전주/금주 표(Task5)·이슈/이벤트(Task5)·본고딕(Task5 FONTS)·DK색(Task1)·A4(Task5)·자산 재사용(Task2)·전주 데이터(Task3)·narrative(Task4)·Excel 무영향(Task3 §불변식)·구덱 제거(Task5/8)·검증(Task6/7/8) 모두 태스크로 존재.
- **Placeholder 스캔:** 코드 스텝은 완전한 코드 포함. `Meeting` 픽스처는 실제 타입에 맞춰 조정하라는 명시적 지시 포함(추정 캐스트임을 표기).
- **타입 일관성:** `buildWeeklyNarrative`/`NarrativeModel{prev,curr,issues,events}`/`NarrativeGroup{phase,items}`/`packGroups`/`Page{prev,curr}` 이름이 Task4·Task5에서 일치. `PhasePlanActual.prevWeek`·`WeeklyMeta.prevWeek*` 이름이 Task3·Task4에서 일치.
- **알려진 리스크(구현 시 확인):** (1) pptxgenjs 4.0.1 타입 인터페이스명(`TextProps`/`TableCell`/`TableCellProps`)이 설치 타입과 다르면 기존 pptx.ts의 인라인 객체 패턴으로 조정. (2) 단일 Phase가 18줄 초과 시 페이지 내 넘침 — 흔치 않음, 필요 시 Phase 내 항목 캡. (3) 본고딕 미설치 환경 대체 렌더.

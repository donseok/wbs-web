# 바이브코딩 경진대회 우승 패키지 — 설계 문서

- 작성일: 2026-07-11
- 상태: 사용자 승인 완료 (섹션 1~4 개별 승인)
- 목표 시점: 2026년 9월 사내 바이브코딩 경진대회 (우승 시 2027년 1월 CES 참관)

## 1. 배경과 목표

D'Flow는 동국씨엠 PI(Process Innovation) 관리를 위해 솔로 바이브코딩으로 구축한
운영 스위트로, 현재 팀이 실제 업무에 사용 중이다. 9월 사내 경진대회(임원급 심사,
라이브 데모 + 발표)에서 우승하기 위해 무엇을 더 만들지를 이 문서로 확정한다.

### 대회 컨텍스트 (클래리파잉 결과)

| 항목 | 확인된 사실 |
|------|------------|
| 대회 성격 | 사내 대회, 임원급 심사 |
| 발표 형식 | 라이브 데모 + 발표 |
| 실사용 | 팀이 실제 업무에 사용 중 (실데이터·실사용자 보유) |
| 제작자 포지션 | IT/개발 유관 직무 — "AI로 생산성을 극대화한 개발자" 서사 |
| AI 예산 | 계속 무료 티어만 (유료 API 불가) |
| 투입 가능량 | 시간 여유 있음 — 크게 간다 |
| 경쟁 구도 | 강력한 경쟁자 존재 — 최고 수준 가정 |

### 핵심 서사

**"기록하는 도구에서 일하는 동료로."** 철강회사 PI 현장에서 실제로 쓰이는 운영
스위트가, 이제 말을 알아듣고 스스로 일한다.

### 현황 진단 (8개 서브시스템 병렬 분석 + 완결성 크리틱 요약)

강점: ① 엑셀→살아있는 WBS 왕복 ② 구글시트급 실시간 동시편집(셀 프레즌스)
③ 원클릭 사내 공식 양식 PPT ④ 환각 없는 무료 티어 RAG 챗봇(429 3중 방어)
⑤ 엔지니어링 규율(테스트 662개, RLS+트리거 이중 방어, 보상 트랜잭션).

약점: ① 챗봇이 읽기 전용 Q&A ② 대시보드가 보고에서 끝남(예측·시뮬레이션 부재)
③ 데모 지뢰(마크다운 별표 노출, 스트리밍 출처 미표시, 토글 숨김, 재임포트 중복,
RPM 20 강등, 스냅샷 이력 부족) ④ 하드코딩 범용성 ⑤ 리포 위생(실명 파일 커밋,
README 드리프트).

대회 자산 발견: 전체 365커밋이 2026-06-25 이후(약 2.5주 솔로 빌드),
설계 문서 17개·구현 계획 12개·멀티에이전트 QA 산출물이 리포에 보존 —
바이브코딩 프로세스의 증거로 활용한다.

## 2. 5분 라이브 데모 아크 (개발 우선순위의 기준)

1. **오프닝 (30초)** — 실제 양식의 (익명화된) 엑셀 업로드 → WBS 트리 원자 생성.
2. **협업 증명 (30초)** — 브라우저 2개, 주간 시트 셀 단위 커서 + 실시간 반영.
3. **센터피스 (2분)** — 음성으로 "ERP 인터페이스 설계 실적 80으로 올려줘" →
   확인 카드 → 적용 → 시트·롤업·대시보드 연쇄 갱신. 회의록 업로드 →
   액션아이템 자동 추출 → 원클릭 WBS 반영.
4. **ROI 클라이맥스 (1분)** — "주간보고 초안 만들어줘" → AI 초안 →
   회사 공식 양식 PPT. "주간보고 30분 → 3분."
5. **임원의 순간 (1분)** — What-if: "이 Phase 2주 지연되면?" →
   예상 준공일·S-Curve·신호등 즉시 재계산.
6. **성과 연결 (30초~1분)** — PI KPI 카드: 기준값 → 목표값 → 현재값,
   기대 편익 → 실현 편익. "공정률이 아니라 회사가 얻은 가치"로 마무리 직전
   격을 높인다. 엔딩은 /story 페이지 — "이 시스템은 자기가 어떻게
   만들어졌는지도 압니다."

마무리 문장(서사 융합): *"D'Flow는 기록하는 도구가 아닙니다. 회의가 끝나는
순간 PI 실행이 시작되고, 실행은 현장의 성과로 이어집니다."*

## 3. Track A — Agentic AI (센터피스)

### A1. 액션 봇: 자연어 명령 → 확인 카드 → 기존 서버 액션

- **명령 파싱**: 기존 의도 분류(src/lib/ai/intent.ts)에 "명령" 경로 추가.
  LLM JSON 단발 호출로 발화를 `{action, target, params}`로 추출.
  지원 액션 카탈로그 v1은 **실적 변경 · 일정 변경 · 완료 처리** 3종으로 한정.
  스트리밍 function calling 대신 JSON 모드 — 기존 llm.ts 모델 체인·429 방어 재사용.
- **대상 해석은 결정형**: LLM이 아니라 팩트시트(buildFactSheet) 기반
  정규화 부분일치 매칭. 후보 다수 → 후보 칩으로 되묻기, 0건 → 정직하게 "못 찾았어요".
- **확인 카드 필수 (자동 실행 없음)**: 봇 말풍선 안에 변경 요약
  (항목명, 필드, 이전값 → 새값)과 [적용]/[취소] 버튼. 적용 시에만 기존 서버 액션
  (updateActual, updateWbsFields 등) 호출 — 권한 게이트·낙관적 잠금·change_logs·
  진척 스냅샷이 전부 그대로 적용된다. 권한 없는 계정의 명령은 기존 에러가
  그대로 노출된다("AI도 권한을 우회하지 못한다"가 데모 포인트).

### A2. 음성 입력

- DkBot 입력창에 push-to-talk 마이크 버튼. 브라우저 내장 Web Speech API(ko-KR),
  중간 인식 결과를 입력창에 실시간 표시. 비용·서버 0.
- 크롬 전용으로 충분(데모 환경 크롬). API 미지원 시 버튼 자체를 숨긴다.
  음성 권한 거부 시 토스트 안내 후 키보드 입력 유지.

### A3. 회의록 → 액션아이템 → WBS 반영

- 회의록 뷰어에 "액션아이템 추출" 버튼. LLM JSON 모드로
  `{결정사항[], 액션아이템[{제목, 담당?, 담당팀?, 기한?, 연관작업?}], 리스크[]}`
  추출. **각 항목에 원문 근거 문장(evidence)을 반드시 포함** — 카드에서 근거를
  펼쳐볼 수 있고, 근거 없는 항목은 표시하지 않는다 (GPT 5.6 교차 검증 채택).
- 추출 결과는 카드 목록으로 표시. WBS 변경을 제안하는 카드는 **변경 전·후
  diff**(기존 값 → 새 값)를 보여주고, [반영]/[반려] 2단 흐름 — 반영 시에만
  기존 서버 액션 호출(권한·change_logs 그대로). A1 확인 카드와 동일 원칙.
- 추출 결과는 신규 테이블(마이그레이션 0025, minute_action_items)에 저장 —
  제안됨/반영됨/반려 상태 추적. "회의가 작업이 되는" 흐름이 이력으로 남는다.

### A4. 주간보고 AI 초안

- 주간 시트에 "AI 초안" 버튼. 서버가 **결정형 재료**(해당 주 change_logs,
  완료/진행 항목, 해당 주 회의록, 차주 예정 항목)를 수집 → LLM이 모듈별
  금주실적/차주계획 문안 작성.
- **빈 셀만 채운다. 사용자 내용은 절대 덮지 않는다.** 초안 셀은 시각적 마커로 구분.
- **근거 표시**: 초안의 각 항목이 어떤 데이터에서 왔는지(WBS 변경 이력/회의록/
  근태) 출처 배지를 달고, 데이터 신선도(마지막 갱신 시점)를 함께 표시
  (GPT 5.6 교차 검증 채택). 추가 출력: 경영진 3줄 요약.
- LLM 실패 시 결정형 불릿 초안(사실 나열)으로 폴백. 이후 기존 PPT 파이프라인 연결.
- ROI 서사: "주간보고 작성 30분 → 3분" — 단, 임의 주장이 아니라 실측 중앙값으로
  제시 (아래 5-2 성과 측정 프로토콜).

### 레이트리밋 전략

A1/A3/A4 모두 상호작용당 LLM 1콜(JSON 단발) — 데모 중 호출 수를 예측·통제
가능. 리허설로 RPM 20 내 페이스 확정.

## 4. Track B — What-if 시뮬레이터

- **위치**: 대시보드 "시뮬레이션" 패널. Phase(또는 서브트리) 선택 +
  슬라이더/±버튼으로 "N일 지연/단축".
- **출력**: 예상 준공일·신호등 판정·S-Curve 계획선을 현재 vs 시뮬레이션
  오버레이로 즉시 재계산.
- **구현**: 순수 도메인 함수(computeTree, plannedAt, buildExecSummary)를
  클라이언트에서 재실행(src/lib/domain/simulate.ts 신설). 서버 왕복·데이터 변경
  없는 100% 샌드박스.
- **정직한 한계 명시**: 선후행 의존관계 모델이 없으므로 "선택 서브트리의
  단순 일정 이동" 시뮬레이션임을 UI에 표기. 의존관계 전파는 범위 제외.

## 5. Track C — 데모 하드닝 + 스토리 자산 (우선순위순)

1. **리포 위생 (최우선·보안)**: 실명 포함 사내 실물 엑셀/PPT를 docs/에서 제거,
   익명화된 데모 시드로 교체. README 전면 재작성(존재하지 않는 데모 모드 안내
   삭제, 현 구조·마이그레이션 24개 반영).
2. **DK Bot 데모 품질**: ① 말풍선 안전 마크다운 렌더(굵게·불릿·줄바꿈만 허용,
   raw HTML 차단 유지) ② 스트리밍 답변에도 출처 칩(회의록 챗봇과 대칭)
   ③ 다크모드·언어 토글 재노출.
3. **엑셀 교체 임포트**: 수동 레시피(삭제 후 재임포트)를 "기존 WBS 교체" UI
   옵션으로 — 데모 중 수정본 재업로드가 안전해진다.
4. **S-Curve 스냅샷 시드**: 데모 프로젝트에 수 주치 진척 이력 시드.
5. **/story — "How it was built" 인앱 페이지**: 2.5주 365커밋 타임라인 차트,
   테스트 662개, 설계 문서 17개·구현 계획 12개, spec→plan→구현→리뷰 프로세스
   도해. 빌드 타임 스크립트로 git log·테스트 수를 정적 데이터로 굽는다.
6. **0021 버킷 DELETE 정책 재적용** (보류분).
7. **알려진 한계는 Q&A 답변으로 준비** (수정하지 않음): 권한 모델의 프로젝트
   스코프 부재, 주간 시트 전원 쓰기 RLS, WBS 시트 가상화 부재, plannedAt 캐시
   부재 등 — 실사용 팀 운영 리스크를 감수하며 고칠 대상이 아니라 정직하게
   설명할 대상.

## 5-1. Track C 추가 — 디자인 마감 패키지 (2026-07-11 디자인 감사 결과)

코드 기반 6영역 병렬 감사 + 프로덕션 실화면 평가(브라우저) 종합 결론:
**디자인 시스템 자체는 상용 SaaS급으로 모던·깔끔** (토큰 2벌 아키텍처, warm
cream+teal 팔레트, 글래스 헤더, 템플릿 냄새 없음). 문제는 시스템이 아니라
**이음새** — 소수의 하드코딩·마감 결함이 심사위원 시선이 먼저 닿는 자리에 있다.
구조 개편 없이 아래 수정으로 "자체 구축한 상용 제품" 첫인상을 굳힌다.

### 빠른 수정 (S — 각 1시간 내외, 3주차에 흡수)

1. 로그인 h1 브랜드명 'DK Flow' → 'D'Flow' 통일 (login/page.tsx:166)
2. WBS 시트 phase/task 행 배경 하드코딩(bg-[#f1f4f9]/[#f8faff]) → 토큰화
   — 라이트에서 쿨톤 이질감, 다크에서 텍스트 소실 (WbsGanttSheet.tsx:594-596)
3. DkBot 전송 버튼 핑크(#ef9a9a) → 브랜드 그라데이션 (DkBot.tsx:341, 대비 2.1:1)
4. 마일스톤 타임라인 '오늘' 마커를 지연 마커(빨간 점)와 시각 분리 + 미니 범례
5. 주간시트 행 삭제 confirm() → 공용 Modal (자체 규약 위반 유일 지점)
6. 로딩 스켈레톤 정합화 — 대시보드 스켈레톤을 현행 레이아웃에 맞추고
   WBS 전용 loading.tsx 추가
7. 홈 화면 영어 혼용 정리 ('ACTIVE RATIO' 등 한국어 화면 내 영어 라벨 3-4곳)

### 패키지 수정 (M — 6~7주차 하드닝에 편입)

8. **회의록 챗 패널 데모 정비 패키지**: 자동 스크롤 부재(스트리밍이 멈춘 듯
   보임), 빈 패널 첫인상(환영 버블+추천 질문 칩), DkBot FAB와 컴포저 겹침,
   로딩 중 Enter 입력 소실, 스크롤 시 패널이 화면에서 사라짐(sticky 미적용
   — 실화면 확인). 같은 파일 2-3개의 수정이라 한 패키지로 처리.
9. 챗봇 마크다운 방어 (기존 Track C-2와 병합): 실화면에서 `**총 4건**` 별표
   노출 **실증 확인됨**. 프롬프트 지시(S) + Bubble 경량 인라인 렌더러(M) 이중.
10. 칸반 드롭 낙관적 업데이트 — 드롭 후 서버 왕복까지 카드가 안 움직여
    시연 하이라이트가 '실패한 듯' 보임. 로컬 오버라이드 + 실패 롤백.
11. WBS 시트↔타임라인 토글 + 줌(전체 기간 fit) — dayPx=24 고정이라 12개월
    간트가 ~8,800px. "전체 일정 한눈에"가 플래그십 화면의 데모 임팩트 최대 투자.
12. Pretendard 폰트 셀프호스팅 — 현재 jsdelivr CDN @import. 심사장 네트워크가
    외부망 확정이면 우선순위 하향 가능, 미확정이면 보험으로 처리.

### 실화면 평가에서 추가된 리스크 메모

- **대시보드 첫 로드 ~10초** (콜드 스타트 + plannedAt 재계산 추정, 실측).
  이후 내비게이션은 3~5초. 데모 직전 전 화면 워밍업을 리허설 체크리스트에
  필수 항목으로 포함. plannedAt 캐시는 기존 결정대로 범위 외 유지하되,
  8주차 버퍼에 여유 시 재평가.
- 전체(글로벌) 스코프 챗봇 답변이 "상세 목록은 확인이 어렵습니다"로 약함
  (실측). 데모 질문은 프로젝트 스코프에서 진행하는 것으로 각본 고정.
  인사말 카피("전체 1개 프로젝트에 대해서도")도 어색 — A1 작업 시 함께 정리.
- 테마 토글 재노출 시(기존 C-2) 서버 prefs 동기화가 로컬 변경을 되돌리지
  않는지 확인 필요 (실화면에서 로컬 다크 강제가 계정 동기화로 롤백됨 — 설계
  의도대로지만 토글 UX 검증 필수).

### 의도적으로 하지 않는 것 (감사 지적됐으나 대회 전 제외)

i18n 전면 정비, 다크모드 전용 이슈 묶음(토글 숨김 상태), 죽은 코드 정리,
접근성 심화(포커스 트랩 추가 등 — 급하게 넣으면 신규 버그 위험), 타이포
스케일 토큰화(178곳 일괄 치환은 시각 회귀 위험), 모바일/태블릿 대응(데스크탑
+프로젝터 데모 기준), 챗봇 컴포넌트 공용화 리팩터링. 근거: 데모 노출 확률
대비 회귀 위험.

## 5-2. Track D — PI 가치 계층 + 관제 강화 (GPT 5.6 교차 검증 채택, 얇게)

GPT 5.6 브레인스토밍의 최대 기여: **"PI의 최종 성과(리드타임·원가·품질 등)가
모델에 없다"**는 지적. 공정률만 보여주면 '잘 만든 PM 도구'로 끝나고, 가치
계층이 있어야 'PI 운영 플랫폼'이 된다. 모두 얇게(thin) 구현한다.

### D1. PI KPI 미니 모델 (마이그레이션 0026)

- KPI 테이블: 이름, 단위, 기준값(baseline), 목표값(target), 현재값(current),
  측정 주기, 데이터 출처(수기 입력), 연결 WBS 항목/마일스톤, 기대 편익·실현
  편익 메모. **현재값은 수기 입력** — 자동 수집은 범위 외.
- 대시보드에 "PI 성과" 카드: KPI별 기준→목표→현재 진행 바 + 연결 WBS 링크.
- 예시 서사: 수주정보 중복 입력 → ERP–MES 인터페이스 개선(WBS) → 수작업
  입력시간 감소(KPI). AS-IS 문제→과제→작업→성과의 한 줄 연결(Digital Thread)
  이 카드 안에서 읽히면 충분 — 전용 시각화 화면은 만들지 않는다.

### D2. 프로젝트 상태 판정 수정 (검증된 결함)

- `projects/page.tsx:36-41 projectStatus()`가 날짜만으로 판정 — 종료일이
  지나면 실적 50%여도 '완료' 표시 (2026-07-11 코드 검증). PI 시스템의 상태
  신뢰도 문제이므로 수정: 날짜 + 실제 WBS 완료율 결합 (예: 기간 경과 & 실적
  <100 → '지연 종료 위험', 실적 100 → '완료'). 사이드바 STATUS_META와 홈
  카드가 같은 판정 함수를 공유하게 순수 함수로 추출.

### D3. 포털 관제 강화 (얇은 Mission Control)

- 프로젝트 카드에 실데이터 칩 추가: 계획 대비 실적(편차), 지연 작업 수,
  다음 마일스톤 D-day, 마지막 업데이트. 기존 exec summary 도메인 함수 재사용.
- 상단 KPI 4장을 위험 중심으로 재구성: 위험 프로젝트 수, 지연 작업 총수,
  임박 마일스톤, 실적 미입력 — '등록된 프로젝트 수' 같은 정적 지표 대체.
- 메뉴 구조 개편(Mission Control 메뉴 신설 등)은 하지 않는다 — 포털 강화로 갈음.

### D4. 대시보드 딥링크 (행동 연결)

- 신호 타일 → 필터된 칸반/WBS(지연만), 마일스톤 점 → 해당 WBS 행 상세,
  알림 항목 → 해당 항목 딥링크(현재 일괄 /kanban 이동을 개선).
- "어디가 위험한가 → 클릭 → 지금 무엇을 해야 하는가" 동선 완성.

### D5. 성과 측정 프로토콜 (1주차부터 즉시 시작)

- 지금부터 실사용으로 수집: 주간보고 작성시간, 회의 종료→WBS 반영시간,
  누락 액션 수, 기한 내 액션 완료율, 엑셀 파일 교환 횟수 등 (측정 시트 운영).
- 발표에서 "90% 단축" 같은 임의 주장 대신 **실제 사용 N회의 중앙값** 제시.
- A3/A4 기능에 처리 시간 로그를 심어 자동 측정 가능한 항목은 자동화.

## 5-3. GPT 5.6 브레인스토밍 교차 검증 기록 (2026-07-11)

docs/competition-brainstorm.md (GPT 5.6 산출물) 분석 결과. 사실관계 정확도
높음 — 365커밋·662테스트·죽은 컴포넌트 4종·README 드리프트·PageHero 등이
본 스펙의 독립 감사와 일치했고, 상태 판정 결함은 코드로 추가 검증됨.

**채택**: PI 가치 계층(D1, 최대 기여), 상태 판정 수정(D2), 포털 관제(D3),
딥링크(D4), 성과 측정 프로토콜(D5), A3 근거·diff·리스크 강화, A4 근거·신선도
·3줄 요약, 서사 융합("회의가 끝나는 순간 PI 실행이 시작됩니다"), 기능 동결
원칙(7주차 이후 신규 기능 금지 + 장애·초기화 리허설), 공고 후 조정 체크리스트
(심사기준별 강조 비율/발표시간별 90초·3분·5분 버전/AI 제한/실데이터 허용 여부
— 공고 발표 시 본 스펙 우선순위 재조정).

**기각/보류 + 근거**:
- 선후행 의존관계 모델 — GPT도 "시간 여유 시"(구현성 3). 기존 non-goal 유지.
  What-if는 단순 이동 유지하되 '지연 vs 회복 비교' 프레이밍만 차용.
- Process Mining — 구현성 2, ERP/MES 실로그 확보 불확실. 기각.
- 메뉴 구조 전면 개편 — 실사용 팀의 학습 비용 + 회귀 위험 > 데모 이득. 기각.
- 역할별 대시보드 프리셋, PMO 분석 컴포넌트 4종 복원 — 8주차 여유 시 스트레치.
- 모바일 카드형 WBS 등 반응형 심화 — 데스크탑+프로젝터 데모 기준 유지.
- 철강 산업 시각 언어 — 디자인 리스크. 카피 통일(로그인·홈 태그라인을 PI
  Control Tower 포지셔닝으로)만 디자인 S 패키지에 포함.
- GPT 문서에 없는 본 스펙 고유 요소는 그대로 유지: A1 자연어/음성 명령
  (GPT의 "챗봇 질문 종류 추가 후순위"는 읽기 전용 Q&A 확장을 말하는 것으로
  본 스펙의 쓰기 명령과 다름), A2 음성, /story 페이지, 디자인 마감 패키지.

## 6. 아키텍처 원칙

- **새 인프라 금지, 기존 자산 재사용**: 명령 파싱·대상 매칭·초안 재료 수집·
  시뮬레이터 수학은 순수 함수 모듈(src/lib/ai/commands/, src/lib/domain/simulate.ts)
  — 기존 도메인/표현 분리 관례 유지.
- 모든 LLM 호출은 기존 llm.ts 체인(429 3중 방어·모델 폴백) 경유, JSON 단발 모드.
- 새 외부 의존성 0개(음성은 브라우저 내장 API).
- 스키마 추가는 0025(minute_action_items) 1개뿐.
- 쓰기는 전부 기존 서버 액션 경유 — AI 경로 전용 쓰기 우회를 만들지 않는다.

## 7. 에러 처리 (기존 "정직한 강등" 철학 유지)

| 상황 | 처리 |
|------|------|
| 명령 파싱 실패 | 예시 문장 안내 |
| 대상 모호 (후보 다수) | 후보 칩으로 되묻기 |
| 대상 0건 | "못 찾았어요" + 검색어 제안 |
| 권한 없음/편집 충돌 | 기존 한국어 에러를 봇 말풍선에 그대로 표출 |
| LLM 불능 (명령) | "지금은 명령 처리가 어려워요" + 수동 UI 안내 |
| LLM 불능 (주간초안) | 결정형 불릿 초안 폴백 |
| LLM 불능 (액션아이템) | 에러 카드 + 재시도 버튼 |
| 음성 권한 거부/무음 | 토스트 안내, 키보드 입력 유지 |

## 8. 테스트 전략

- 순수 함수(명령 카탈로그 검증, 대상 매처, 시뮬레이터 수학, 초안 재료 빌더):
  vitest 단위 테스트 — 기존 tests/ 스타일.
- 서버 액션 권한 게이트: 기존 accounts-gate 패턴의 모킹 테스트
  (게이트 통과 전 쓰기 도달 금지 스파이).
- 런타임 검증: verify 스킬(빌드/린트/테스트 + curl), 프로덕션 배포 후 실사용 확인.
- 데모 리허설 체크리스트 문서를 7주차에 작성, 2회 리허설.

## 9. 8주 일정

| 주차 | 내용 |
|------|------|
| 1–2주 | A1 액션 봇 (파싱→매칭→확인 카드→3종 액션) + C1 리포 위생·README + D5 측정 시작 + D2 상태 판정 수정 |
| 3주 | A2 음성 입력 + C2 DK Bot 데모 품질 (마크다운·출처·토글) + 디자인 S 수정 7건(+PI 카피 통일) |
| 4주 | A3 회의록→액션아이템 (0025, 근거·diff·리스크 포함) |
| 5주 | A4 주간보고 AI 초안 (근거·신선도·3줄 요약 포함) |
| 6주 | Track B What-if + D1 PI KPI 미니 모델(0026) + 스냅샷 시드 + 엑셀 교체 임포트 + 디자인 M 수정(칸반 낙관 업데이트·시트/타임라인 토글) |
| 7주 | /story 페이지 + D3 포털 관제 + D4 딥링크 + 데모 리허설 1차 + 잔여 하드닝(회의록 챗 패널 패키지·폰트 셀프호스팅). **7주차 종료 시 기능 동결** |
| 8주 | 버퍼(스트레치: PMO 분석 복원) + 리허설 2차(장애·데이터 초기화 시나리오 포함) + 발표 준비 |

완료 기준: 각 기능은 프로덕션 배포 + 팀 실사용까지. "대회용 기능"이 아니라
"실사용 증거"를 쌓는다. 7주차 이후 신규 기능 금지(기능 동결) — 8주차는
안정화·리허설 전용.

## 10. 범위 제외 (Non-goals)

- 선후행 의존관계 모델·크리티컬 패스 (데이터 모델 신설 필요, What-if는 단순
  일정 이동으로 충분 — GPT 5.6 교차 검증에서도 동일 결론)
- Process Mining (ERP/MES 실로그 확보 불확실)
- 메뉴 구조 전면 개편, 역할별 대시보드 프리셋 (프리셋은 8주차 스트레치)
- KPI 현재값 자동 수집 (수기 입력으로 시작 — ERP/MES 연동은 대회 후)
- 권한 모델 프로젝트 스코프 개편 (실사용 운영 리스크, Q&A 대비로 갈음)
- WBS 시트 가상화, plannedAt 캐시 (데모 규모에서 비문제)
- 유료 API·외부 STT (무료 티어 제약 유지)
- i18n 전체 완역 (기존 보류 결정 유지 — 데모는 한국어 진행)
- 명령 카탈로그 확장 (회의 생성·공지 작성·담당 변경 등은 v1 이후)

## 11. 리스크와 완화

| 리스크 | 완화 |
|--------|------|
| 데모 현장 RPM 20 초과 → lite 강등 | 상호작용당 1콜 설계 + 리허설로 페이스 확정 + 폴백도 데모 가능한 품질 유지 |
| 음성 인식 오류 (현장 소음) | push-to-talk + 인식 결과가 입력창에 먼저 보이고 전송은 수동 — 오인식이 그대로 실행되지 않음 |
| 확인 카드 UX가 "번거로움"으로 보일 위험 | "AI가 권한과 감사 이력을 우회하지 않는다"를 발표 서사로 반전 |
| 병렬 세션 git 충돌 | git add에 파일 명시 (기존 규칙 유지, add -A 금지) |
| 실사용 팀에 미완성 기능 노출 | 기능 플래그 또는 완성 단위 배포 — 주 단위 완결 배포 원칙 |
| 대시보드 콜드 스타트 ~10초 (실측) | 데모 직전 전 화면 워밍업을 리허설 체크리스트 필수 항목화 |
